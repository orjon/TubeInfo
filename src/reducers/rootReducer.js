const initialState = {
  lineStatuses : [
    {id:1, msg: '111111'},
    {id:2, msg: '222222'},
    {id:3, msg: '333333'} 
  ]
}

const rootReducer = (state = initialState, action) => {
  return state
}

export default rootReducer